export interface WishlistItem {
  id: string;
  name: string;
  badge?: string;
  image: string;
  rating: number;
  reviewCount: number;
  weight: string;
  dimensions: string;
  price: number;
  originalPrice: number;
  currency: string;
}
